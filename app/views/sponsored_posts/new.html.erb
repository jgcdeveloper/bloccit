<h1>New Sponsored Post</h1>

 <div class="row">

   <div class="col-md-4">
     <p>Guidelines for posts</p>
     <ul>
       <li>Don't Forget the Price</li>
       <li>They paid... they don't really have rules to follow</li>
       <li>Do follow societal norms however</li>
     </ul>

   </div>

   <div class="col-md-8">
     <%= form_for [@topic, @sponsored_post] do |f| %>

       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, required: true, class: 'form-control', placeholder: "Enter sponsored post title" %>
       </div>

      <div class="form-group">
         <%= f.label :price %>
         <%= f.number_field :price, min: 0, max: 50, class: 'form-control', placeholder: "Enter sponsored post price between 0 and 50" %>
      </div>

       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, required: true, rows: 8, class: 'form-control', placeholder: "Enter sponsored post text body" %>
       </div>

       <div class="form-group">
          <%= f.submit "Save", class: 'btn btn-success' %>
       </div>

     <% end %>

   </div>

 </div>
